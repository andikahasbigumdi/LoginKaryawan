<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.training.day1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".andikahasbigumdi"></activity>
        <activity android:name=".OfficeDetailActivity" />
        <activity android:name=".DeveloperActivity" />
        <activity android:name=".KantorActivity" />
        <activity android:name=".About" />
        <activity android:name=".DataKantor" />
        <activity
            android:name=".AboutActivity"
            android:label="About" />
        <activity
            android:name=".EmployeeActivity"
            android:label="Data Karyawan" />
        <activity android:name=".ForgotPassword" />
        <activity
            android:name=".MainActivity"
            android:label="Home"
            android:screenOrientation="portrait"/>
        <activity android:name=".LoginActivity" android:screenOrientation="portrait"/>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>